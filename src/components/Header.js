import React from "react";
import StyledHeader from "../styled-components.js/StyledHeader";

const Header = () => {
  return (
    <div className="header">
      <StyledHeader>Northcoders Student System</StyledHeader>
    </div>
  );
};

export default Header;
